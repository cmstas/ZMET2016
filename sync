if [[ $1 == "push" ]]
then
  uaf_dir=/home/users/bhashemi/Projects/GIT/ZMET2016
  
  if [[ `git rev-parse --abbrev-ref HEAD` == "CMS4" ]]
  then
    uaf_dir=/home/users/bhashemi/Projects/GIT/CMS4/ZMET2016
  fi

	git ls-files --exclude-standard > transfer_files.tmp
	rsync -avP --files-from=transfer_files.tmp . uaf-10:$uaf_dir
	rm transfer_files.tmp

elif [[ $1 == "pull" ]]
then
	echo "Don't know how to pull yet"
else
	echo "must pass push or pull"
fi

